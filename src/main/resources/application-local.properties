
# Database
spring.datasource.password=root

# JWT secret
jwt.secret=${JWT_SECRET:ClaveTemporalWirtz2026_DebeSerMasLargaParaQueNoFalle}

# Finnhub API key — set FINNHUB_API_KEY env var in production, use real key here for local dev
finnhub.api.key=d74n4d1r01qg1eo64d4gd74n4d1r01qg1eo64d50

# Twelve Data API key
twelve.api.key=${TWELVE_API_KEY:}

# Debug logging (local only — not included in application.properties)
logging.level.org.springframework.security=DEBUG
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
